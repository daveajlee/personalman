name: Build the new Desktop Client (Electron)

on:
  push:
    branches: [ main ]
    paths:
      - "desktop/**"
  pull_request:
    branches: [ main ]
    paths:
      - "desktop/**"
  workflow_dispatch:

jobs:
    build:
      runs-on: ubuntu-latest

      steps:
        - uses: actions/checkout@v2

        - name: Use Node.js 22.x
          uses: actions/setup-node@v3
          with:
            node-version: 22.x
            cache: 'npm'
            cache-dependency-path: server/package-lock.json
        - run: cd server && npm ci
        - run: cd server && npm run build --if-present
        - run: cd server && npm run test:nowatch